[pytest]
addopts = -q
pythonpath = .
testpaths = tests
markers =
    performance: marks tests as performance tests (deselect with '-m "not performance"')
    unit: fast, isolated unit tests
    integration: tests that integrate with external services or multiple modules
    e2e: end-to-end API level tests
    contract: legacy or deprecated tests kept for reference
